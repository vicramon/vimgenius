%aside
  %ul.levels
    %li{ class: (params[:id] == "vim-intro" ? "active" : "complete")}
      %p Intro
    - lesson.levels.each do |level|
      %li{ class: level_class_selector(params, level) }
        %p= level.name
