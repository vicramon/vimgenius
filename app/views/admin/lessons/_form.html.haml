= form_for lesson, url: admin_lesson_path(lesson) do |f|
  %fieldset

    %dl
      %dt= f.label :name
      %dd= f.text_field :name

    %dl
      %dt= f.label :summary
      %dd= f.text_area :summary

    %dl
      %dt= f.label :directions
      %dd= f.text_area :directions

  %fieldset.actions
    = f.submit

